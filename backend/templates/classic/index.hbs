<!DOCTYPE html>
<html>
  <head>
    <title>{{name}} - Welcome!</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <img src="{{logoUrl}}" alt="Hotel Logo" style="max-width:200px;">
    <h1>Welcome to {{name}}</h1>
    <p>Contact: {{contact.email}} | {{contact.phone}}</p>
    <p>Address: {{contact.address}}</p>
  </body>
</html>